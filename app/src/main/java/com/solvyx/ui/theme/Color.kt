package com.solvyx.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta principal Solvyx ──────────────────────────
val TealPrimary       = Color(0xFF1D9E75)  // Botones, headers, elementos activos
val TealMedium        = Color(0xFF4C8B6E)  // Textos secundarios, bordes
val TealLight         = Color(0xFFB8DDC7)  // Bordes suaves, shapes decorativos
val TealLightest      = Color(0xFFFEFEFE)  // Fondos de cards, chips activos
val TealDark          = Color(0xFF1B5B4C)  // Textos principales, outlines de Berto
val BackgroundApp     = Color(0xFFF9F8F6)  // Fondo general de la app

// ── Crisis / SOS ────────────────────────────────────
val CrisisRed         = Color(0xFFE24B4A)  // Botón SOS, estados de crisis
val CrisisRedLight    = Color(0xFFfde8e8)  // Fondo suave en pantalla de crisis
val CrisisRedDark     = Color(0xFF991b1b)  // Texto sobre fondo rojo claro

// ── Berto estados ────────────────────────────────────
val BertoVisorCalm    = Color(0xFFE1F5EE)  // Tranquilo
val BertoVisorCelebr  = Color(0xFFd4f7e0)  // Celebrando
val BertoVisorWorried = Color(0xFFfef9c3)  // Preocupado
val BertoVisorCrisis  = Color(0xFFfde8e8)  // Crisis
val BertoBodyCrisis   = Color(0xFFB4B2A9)  // Cuerpo gris en crisis

// ── Neutros ─────────────────────────────────────────
val White             = Color(0xFFFFFFFF)
val TextMuted         = Color(0xFF888780)
val CardBorder        = Color(0xFFD1FAE5)

// ── Advertencia (ansiedad, preocupacion) ─────────────
val WarnAmber         = Color(0xFFfef9c3)
val WarnAmberDark     = Color(0xFF92400e)

// ── Racha ─────────────────────────────────────────────
val StreakFlame       = Color(0xFFE8823C)  // Ícono de flama en la tarjeta de racha

// ── Ánimo (bitácora + estado_animo de Berto) ─────────
val MoodTriste         = Color(0xFF6B93D6)
val MoodAnsioso        = Color(0xFFE3A23C)
val MoodNeutral        = Color(0xFF5FA895)
val MoodBien           = Color(0xFF4C9F6E)
val MoodEuforico       = Color(0xFFD9679A)